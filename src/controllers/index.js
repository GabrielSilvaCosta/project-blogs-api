const loginController = require('./login.controller');
const userController = require('./user.controler');
const categoryControler = require('./category.controler');
const blogPostController = require('./post.controller');

module.exports = {
  loginController,
  userController,
  categoryControler,
  blogPostController,
  
};